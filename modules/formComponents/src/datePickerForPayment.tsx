

export function a(){
//idea - we have a select fn that checks the date and if there is a problem has a side effect
  //the sideeffect might be to open a modal page. It might be to send a message... not clear... so lets do both
  //It needs
}