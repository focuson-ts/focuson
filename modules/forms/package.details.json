{
  "template":    "forms",
  "name":        "@focuson/forms",
  "description": "Allows us to define data that is stored in a database, or perhaps an api",
  "links":       [
    "@focuson/utils",
    "@focuson/files",
    "@focuson/focuson"
  ],
  "guards":      {
    "publish":   true,
    "generator": true,
    "javaPort":  8080,
    "tsPort":    3000
  }
}